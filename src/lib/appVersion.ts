/**
 * App version shown in the About section. Kept in sync with package.json
 * manually (a build-time import of package.json into the client bundle is
 * avoidable churn for a single string).
 */
export const APP_VERSION = "0.1.0";

/** Where product feedback goes. Replace before launch. */
export const FEEDBACK_EMAIL = "feedback@ourpantry.app";
